<header>    
    <div class="page-header">     
      <h1 class = 'storeTitle'> Bath & Company</h1>  
             
        <div class="navigation">    
          <nav role='navigation'>
            <ul  >
                  
               <li><%= link_to 'Home', root_path %></li>
                <li><%=link_to 'Admin', admin_root_path %></li>     
                            
                 <% @pages.each do |page| %>
                    <li><%= link_to page.name, category_path(page) %></li>                      
                  <% end %>  
                     
                  <% @links.each do |page| %>
                       <li><%= link_to page.title, link_path(page) %></li>                     
                  <% end %>
                            
             </ul>
          </nav>
        </div>
           
    </div>
</header>

 