 <h1> Search Results for <%= params[ :search_keywords] %></h1>
 
 <% if params[ :search_keywords].empty? %>
      <p>No Keywords</p>
    <%else%>
    <% if @products.size.zero? %>
      <p>No products found</p>
      <%else%>
        <p><%= pluralize(@products.size, 'product') %> found</p>
        <%= render @products %>
     <%end%>
  <%end%>
  
  <h3> Products under this category are... </h3>
  
 



 



 